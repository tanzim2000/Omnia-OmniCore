{
	"settings": [
		{
			"key": "market",
			"label": "Region",
			"type": "select",
			"options": ["en-CA", "en-US", "en-GB", "en-IN", "ja-JP", "de-DE"],
			"default": "en-CA",
			"help": "Bing shows a different picture per region"
		},
		{
			"key": "refreshMinutes",
			"label": "Refresh every",
			"type": "number",
			"default": 180,
			"help": "The picture changes once a day, so this can be long"
		}
	]
}