{
	"settings": [
		{
			"key": "size",
			"label": "Tile size",
			"type": "select",
			"options": ["Hidden", "Small", "Medium", "Wide", "Large"],
			"default": "Wide",
			"hiddenValue": "Hidden",
			"help": "How much room this gets, and therefore how much of its content fits. Hidden keeps the module running without putting it on screen."
		}
	]
}